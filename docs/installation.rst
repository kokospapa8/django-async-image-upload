============
Installation
============

At the command line::

    $ easy_install django-async-image-upload

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-async-image-upload
    $ pip install django-async-image-upload
